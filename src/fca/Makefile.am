noinst_LIBRARIES   	= lib_fca.a

lib_fca_a_SOURCES 	= context.h context.c concept.h concept.c sorted_context.c sorted_context.h

lib_fca_a_LIBADD	= ../bitset/bitset.o

CLEANFILES      	= *.o
DISTCLEANFILES  	= .deps Makefile